# Minimal requirements for basic functionality
# Use this for faster deployment if you encounter issues

# Core web framework
streamlit>=1.28.0

# HTTP requests and data processing
requests>=2.31.0
pandas>=2.0.3
numpy>=1.26.0

# Basic ML (without heavy dependencies)
scikit-learn>=1.3.0
google-generativeai>=0.3.2

# PDF processing (required for Q&A)
PyPDF2>=3.0.1

# Text processing
beautifulsoup4>=4.13.4
lxml>=6.0.0

# Data visualization
plotly>=6.2.0

# Environment management
python-dotenv>=1.0.0

# Note: This minimal version excludes:
# - torch (large download)
# - sentence-transformers (depends on torch)
# - transformers (depends on torch)
# - faiss-cpu (vector storage)
# - keybert (keyword extraction)
# - nltk (NLP utilities)
# - OCR capabilities

# Some features may be limited with this minimal setup
